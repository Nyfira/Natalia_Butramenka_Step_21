// Task 1
class Marker {
    constructor(color, ink) {
        this.color = color;
        this.ink = ink;
    };

    SetText(text) {

    }
}

let string1 = new Marker(`red`, `100%`);
string1.SetText();

document.write(string1.SetText());